# EZ_Account/backup_test_files

This top-level directory is now only a pointer.

The archived exploratory account tests previously stored here now live under:

- `EZ_V1/EZ_Account/legacy_tests_backup/`

They are historical/debugging material, not the maintained V1 test entry point.
