# EZ_V1.EZ_Units

This is the physical home of the V1 shared-units subsystem after staged
migration under `EZ_V1/`.

The top-level `EZ_Units/` package remains as a compatibility shim so
historical imports continue to work during the phased move.
