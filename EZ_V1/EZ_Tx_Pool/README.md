# EZ_V1.EZ_Tx_Pool

This is the physical home of the V1 transaction-pool subsystem after staged
migration under `EZ_V1/`.

The top-level `EZ_Tx_Pool/` package remains as a compatibility shim so
historical imports continue to work during the phased move.
