# EZ_V1/EZ_Account

This directory contains the physical V1 account implementation after staged
migration under `EZ_V1/`.

Top-level imports such as `EZ_Account.Account` are still supported through
compatibility shims so historical callers do not need to change immediately.
