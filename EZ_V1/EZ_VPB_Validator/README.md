# EZ_V1.EZ_VPB_Validator

This is the physical home of the V1 VPB validator subsystem after staged
migration under `EZ_V1/`.

The top-level `EZ_VPB_Validator/` package remains as a compatibility shim so
historical imports continue to work during the phased move.
