# EZ_V1.EZ_Miner

This is the physical home of the V1 miner helper after the first staged
migration under `EZ_V1/`.

The top-level `EZ_Miner/` package remains as a compatibility shim so that
historical imports continue to work.
