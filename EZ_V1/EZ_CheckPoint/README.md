# EZ_V1.EZ_CheckPoint

This is the physical home of the V1 checkpoint subsystem after staged
migration under `EZ_V1/`.

The top-level `EZ_CheckPoint/` package remains as a compatibility shim so
historical imports continue to work during the phased move.
