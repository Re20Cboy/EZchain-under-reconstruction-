# EZ_V1/EZ_Transaction

This directory contains the physical V1 transaction implementation after staged
migration under `EZ_V1/`.

Top-level imports such as `EZ_Transaction.SingleTransaction` and
`EZ_Transaction.MultiTransactions` remain available through compatibility shims.
