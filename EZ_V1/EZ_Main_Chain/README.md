# EZ_V1.EZ_Main_Chain

This is the physical home of the V1 main-chain subsystem after staged
migration under `EZ_V1/`.

The top-level `EZ_Main_Chain/` package remains as a compatibility shim so
historical imports continue to work during the phased move.
